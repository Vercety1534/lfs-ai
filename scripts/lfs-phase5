#!/usr/bin/env bash
# LFS-AI - LFS Install Script
# Phase: LFS Phase 5
# Purpose: Install phase 5 of Linux from Scratch
# Safe rerun: Yes
# Copyright (c) 2026 Michael Femiak (Vercety1534)
# SPDX-License-Identifier: MIT

set -euo pipefail

RED=$'\033[31m'
RESET=$'\033[0m'

cd /lfs-ai

# Error handling
die() {
    printf '%sERROR:%s %s\n' "$RED" "$RESET" "$1" >&2
    exit 1
}

rm -rf /logs/lfs/phase5
mkdir -p /logs/lfs/phase5

[ -d /lfs-ai/lfs ] || die 'Missing /lfs-ai/lfs'

# Phase 5 install
shopt -s nullglob

found_script=0
for script in /lfs-ai/lfs/{075..104}-*; do
    if [[ -f "$script" && -x "$script" ]]; then
        found_script=1
        bash "$script"
    fi
done

if (( found_script != 1 )); then
    die 'No executable phase 5 scripts found in /lfs-ai/lfs'
fi
